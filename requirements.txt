# Flask 核心框架（web 服务）
flask>=2.3.3,<3.0.0

# 跨域支持
flask-cors>=4.0.0,<5.0.0

# 可选：如果项目用到 JSON 解析/数据处理（按需添加）
requests>=2.31.0,<3.0.0
pyyaml>=6.0.1,<7.0.0

# 可选：如果项目用到日志/配置（按需添加）
python-dotenv>=1.0.0,<2.0.0